global:
  scrape_interval: 5s

scrape_configs:

  - job_name: "aegisops"
    static_configs:
      - targets:
          - "host.docker.internal:5000"

  - job_name: "reliefgrid"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets:
          - "host.docker.internal:8080"